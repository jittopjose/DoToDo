import { ChecklistSummaryDisplayPipe } from './checklist-summary-display.pipe';

describe('ChecklistSummaryDisplayPipe', () => {
  it('create an instance', () => {
    const pipe = new ChecklistSummaryDisplayPipe();
    expect(pipe).toBeTruthy();
  });
});
