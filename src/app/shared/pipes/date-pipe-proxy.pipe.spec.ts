import { DateProxyPipe } from './date-proxy.pipe';

describe('DateProxyPipe', () => {
  it('create an instance', () => {
    const pipe = new DateProxyPipe();
    expect(pipe).toBeTruthy();
  });
});
