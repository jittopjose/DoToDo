import { TaskStatusDisplayPipe } from './task-status-display.pipe';

describe('TaskStatusDisplayPipe', () => {
  it('create an instance', () => {
    const pipe = new TaskStatusDisplayPipe();
    expect(pipe).toBeTruthy();
  });
});
