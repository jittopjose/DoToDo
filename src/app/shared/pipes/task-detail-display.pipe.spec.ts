import { TaskDetailDisplayPipe } from './task-detail-display.pipe';

describe('TaskDetailDisplayPipe', () => {
  it('create an instance', () => {
    const pipe = new TaskDetailDisplayPipe();
    expect(pipe).toBeTruthy();
  });
});
