import { ChecklistStatusDisplayPipe } from './checklist-status-display.pipe';

describe('ChecklistStatusDisplayPipe', () => {
  it('create an instance', () => {
    const pipe = new ChecklistStatusDisplayPipe();
    expect(pipe).toBeTruthy();
  });
});
